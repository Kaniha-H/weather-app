{% extends 'base.html.twig' %}

{% block title %}Weather{% endblock %}

{% block body %}
    <h1>Weather</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ weather.id }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ weather.city }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ weather.country }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ weather.lat }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ weather.lon }}</td>
            </tr>
            <tr>
                <th>Temperature</th>
                <td>{{ weather.temperature }}</td>
            </tr>
            <tr>
                <th>humidité</th>
                <td>{{ weather.humidity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('weather') }}">back to list</a>
    <a href="{{ path('weather_edit', {'id': weather.id}) }}">modifier</a>
    {{ include('weather/_delete_form.html.twig') }}
{% endblock %}
